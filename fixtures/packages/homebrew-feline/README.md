# homebrew-feline

A sample homebrew extension package: a feline species with a puma subspecies
and a custom "bruised lung" condition. It exists to prove the extension path
(a package that adds a species and a condition on top of the `srd51` base
without touching it) and is a dependency of the reference Monk fixture.
