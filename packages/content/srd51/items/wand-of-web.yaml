# Generated by tools/import (map stage) from the SRD 5.1 datasets pinned in tools/import/sources.lock.yaml.
# Mechanics come from tools/import/overlay/<entity-id>.yaml; edit those, then re-run `node tools/import/src/map.ts`.
id: srd51.item.wand-of-web
name:
  en: Wand of Web
source: srd51
text:
  en: |-
    Wand, uncommon (requires attunement by a spellcaster)

    This wand has 7 charges. While holding it, you can use an action to expend 1 of its charges to cast the web spell (save DC 15) from it.

    The wand regains 1d6 + 1 expended charges daily at dawn. If you expend the wand's last charge, roll a d20. On a 1, the wand crumbles into ashes and is destroyed.
type: wondrous
magical: true
rarity: uncommon
attunement:
  by:
    en: a spellcaster
tags:
  - magic-item
  - wand
charges:
  max: 7
  recharge:
    - on: dawn
      amount: 1d6 + 1
features:
  - id: srd51.feature.item.wand-of-web.web
    name:
      en: Web
    effects:
      - kind: grant-spells
        spells:
          - srd51.spell.web
        as: always-prepared
        cost:
          - resource: charges
            amount: 1
        note:
          en: "Save DC 15; cast as an action while holding the wand. If you expend the last charge, roll a d20: on a 1 the wand crumbles into ashes and is destroyed."
