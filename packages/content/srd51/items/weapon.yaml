# Generated by tools/import (map stage) from the SRD 5.1 datasets pinned in tools/import/sources.lock.yaml.
# Mechanics come from tools/import/overlay/<entity-id>.yaml; edit those, then re-run `node tools/import/src/map.ts`.
id: srd51.item.weapon
name:
  en: Weapon, +1, +2, or +3
source: srd51
text:
  en: |-
    Weapon (any), uncommon (+1), rare (+2), or very rare (+3)

    You have a bonus to attack and damage rolls made with this magic weapon. The bonus is determined by the weapon's rarity.
type: weapon
magical: true
tags:
  - magic-item
  - weapon
features:
  - id: srd51.feature.item.weapon.bonus
    name:
      en: Magic Weapon
    effects:
      - kind: modify-attacks
        filter:
          item: srd51.item.weapon
        set:
          attackBonus: 1
          damageBonus: 1
          magical: true
        note:
          en: "The bonus is determined by the weapon's rarity: +1 (uncommon), +2 (rare) or +3 (very rare). This entity carries the +1 value and no base weapon properties of its own; the other variants need the value changed."
