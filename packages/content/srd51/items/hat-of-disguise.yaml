# Generated by tools/import (map stage) from the SRD 5.1 datasets pinned in tools/import/sources.lock.yaml.
# Mechanics come from tools/import/overlay/<entity-id>.yaml; edit those, then re-run `node tools/import/src/map.ts`.
id: srd51.item.hat-of-disguise
name:
  en: Hat of Disguise
source: srd51
text:
  en: |-
    Wondrous item, uncommon (requires attunement)

    While wearing this hat, you can use an action to cast the disguise self spell from it at will. The spell ends if the hat is removed.
type: wondrous
magical: true
rarity: uncommon
attunement: true
tags:
  - magic-item
  - wondrous-items
features:
  - id: srd51.feature.item.hat-of-disguise.disguise-self
    name:
      en: Disguise Self
    text:
      en: While wearing this hat, you can use an action to cast the disguise self spell from it at will. The spell ends if the hat is removed.
    effects:
      - kind: grant-spells
        spells:
          - srd51.spell.disguise-self
        as: always-prepared
        note:
          en: Cast at will as an action, without a spell slot. The spell ends if the hat is removed.
