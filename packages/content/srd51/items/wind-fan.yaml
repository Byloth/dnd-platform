# Generated by tools/import (map stage) from the SRD 5.1 datasets pinned in tools/import/sources.lock.yaml.
# Mechanics come from tools/import/overlay/<entity-id>.yaml; edit those, then re-run `node tools/import/src/map.ts`.
id: srd51.item.wind-fan
name:
  en: Wind Fan
source: srd51
text:
  en: |-
    Wondrous item, uncommon

    While holding this fan, you can use an action to cast the gust of wind spell (save DC 13) from it. Once used, the fan shouldn't be used again until the next dawn. Each time it is used again before then, it has a cumulative 20 percent chance of not working and tearing into useless, nonmagical tatters.
type: wondrous
magical: true
rarity: uncommon
tags:
  - magic-item
  - wondrous-items
features:
  - id: srd51.feature.item.wind-fan.gust-of-wind
    name:
      en: Gust of Wind
    effects:
      - kind: grant-spells
        spells:
          - srd51.spell.gust-of-wind
        as: always-prepared
        uses:
          count: 1
          recharge: dawn
        note:
          en: "Save DC 13; cast as an action while holding the fan. Once used, the fan shouldn't be used again until the next dawn: each further use before then has a cumulative 20 percent chance of not working and tearing the fan into useless, nonmagical tatters."
