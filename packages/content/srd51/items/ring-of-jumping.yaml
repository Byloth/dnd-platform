# Generated by tools/import (map stage) from the SRD 5.1 datasets pinned in tools/import/sources.lock.yaml.
# Mechanics come from tools/import/overlay/<entity-id>.yaml; edit those, then re-run `node tools/import/src/map.ts`.
id: srd51.item.ring-of-jumping
name:
  en: Ring of Jumping
source: srd51
text:
  en: |-
    Ring, uncommon (requires attunement)

    While wearing this ring, you can cast the jump spell from it as a bonus action at will, but can target only yourself when you do so.
type: wondrous
magical: true
rarity: uncommon
attunement: true
tags:
  - magic-item
  - ring
features:
  - id: srd51.feature.item.ring-of-jumping.jump
    name:
      en: Jump
    text:
      en: While wearing this ring, you can cast the jump spell from it as a bonus action at will, but can target only yourself when you do so.
    effects:
      - kind: grant-spells
        spells:
          - srd51.spell.jump
        as: always-prepared
        note:
          en: Cast at will as a bonus action, without a spell slot, targeting only yourself.
