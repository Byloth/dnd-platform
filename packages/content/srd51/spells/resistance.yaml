# Generated by tools/import (map stage) from the SRD 5.1 datasets pinned in tools/import/sources.lock.yaml.
# Mechanics come from tools/import/overlay/<entity-id>.yaml; edit those, then re-run `node tools/import/src/map.ts`.
id: srd51.spell.resistance
name:
  en: Resistance
source: srd51
level: 0
school: abjuration
castingTime:
  activation: action
range:
  type: touch
components:
  v: true
  s: true
  m:
    en: A miniature cloak.
duration:
  type: timed
  minutes: 1
  concentration: true
text:
  en: You touch one willing creature. Once before the spell ends, the target can roll a d4 and add the number rolled to one saving throw of its choice. It can roll the die before or after making the saving throw. The spell then ends.
effects:
  - kind: modify
    target: save.all
    op: add
    value: 1d4
    note:
      en: Once only, on one saving throw of the target's choice, rolled before or after the save; the spell then ends.
